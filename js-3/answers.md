1.Object in javascript is the pairs of keys and values pairs.This pairs are named properties.Properties can be  a function,array,object,string and so on.   
 and objects contain all  data types, such as strings, numbers, and booleans.
 So functions in object are called  methods.